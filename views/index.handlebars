<div class="container">

    <div class="row One">

        <h1> BURGERS</h1>


    </div>

    <div class="row Two">

        <div class="col-md-4 notDevoured">
            <ul>
                {{#each burgers}}
                {{#unless devoured}}

                <li>
                    <p>
                        {{this.burger_name}}
                        <button data-planid="{{this.id}}" class="devour">Devour it!</button>
                    </p>
                </li>
                {{!-- 
                {{#unless devoured}}
                <p>
                    {{this.burger_name}}
                    <button data-planid={{"this.id"}} class="devour">Devour it! </button>

                </p>

                {{/unless}} --}}
                {{/unless}}

                {{/each}}

            </ul>


        </div>

        <div class="col-md-4 burgerImage">

            <img src="./assets/images/BurgerImg.png" alt="Burger image">
        </div>

        <div class="col-md-4 devoured">

            {{#each burgers}}

            {{#if devoured}}

            <p>
                <li>
                    {{this.burger_name}}
                </li>
            </p>


            {{/if}}


            {{/each}}

        </div>
    </div>

    <br>
    <br>

    <div class="row Three">

        {{!-- <form class="create-form" action="/" method="POST">

            <div class="col-lg-6">
                <textarea id="ca" type="text" name="burger"></textarea>
            </div>

            <div class="col-lg-6">
                <button type="submit" id="submit">Submit</button>

            </div>

        </form> --}}
        <div class="col-6-md write">

            <form id="createburger" class="create-form">

                <div class="col-md-6 form-group">
                    <textarea id="burgerinput" type="text" name="burger"></textarea>

                </div>

                <div class="col-md-6">

                    <button id="burgerSubmit" type="submit">Add Burger</button>
                </div>

            </form>
        </div>




    </div>

</div>